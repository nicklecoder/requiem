---
id: add-warns-but-never-refuses
rejected_at: 2026-09-15T23:50:00.931150611Z
see_instead: model/add-checks-before-writing
---

Have add print its near-matches on stderr and write regardless, keeping every gate out of the tool. Rejected: that is precisely what the workflow already asked an agent to do by hand, and the field measured the result — 54 duplicates in 255 statements, with a hand-built loader compensating for the missing step. A warning on a path that succeeds anyway is indistinguishable from no warning once an agent works at volume. Refusing with an override preserves what the warning was protecting, since the agent proceeds with one flag.
