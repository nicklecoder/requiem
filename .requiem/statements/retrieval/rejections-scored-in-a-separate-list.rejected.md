---
id: rejections-scored-in-a-separate-list
rejected_at: 2026-09-15T23:35:46.106986413Z
see_instead: retrieval/rejections-embedded
---

Leave rejections unembedded and return them as a second, separately scored list instead, so statements cannot outrank them. Rejected: it requires a wrapper object around check output, which breaks the no-envelope convention every caller depends on — the same ground cli/coverage-envelope was rejected on — and it treats the symptom, since a rejection with no vector stays invisible to meaning-based search however the results are arranged.
